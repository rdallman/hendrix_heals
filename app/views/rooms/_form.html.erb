<%= form_for(@room) do |f| %>
  <% if @room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% @room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br>
    <%= f.collection_select :id, Room.all, :id, :number %>
  </div>
  <div class="field">
    <%= f.label :bed %><br>
    <%= f.select :bed, ["A","B"] %>
  </div>
  <div class="field">
    <%= f.label :privacy %><br>
    <%= f.select :privacy, ["SP","PR"] %>
  </div>
  <div class="field">
    <%= f.label :patient %><br>
    <%= f.collection_select :patient_id, Patient.all, :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

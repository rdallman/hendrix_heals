<h1>Listing procedures</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Category</th>
      <th>Cost</th>
      <th>Time</th>
      <th>Physician</th>
      <th>Patient</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @procedures.each do |procedure| %>
      <tr>
        <td><%= procedure.try(:name) %></td>
        <td><%= procedure.try(:category) %></td>
        <td><%= procedure.try(:cost) %></td>
        <td><%= procedure.try(:time) %></td>
        <td><%= procedure.physician.try(:name) %></td>
        <td><%= procedure.patient.try(:name) %></td>
        <td><%= link_to 'Show', procedure %></td>
        <td><%= link_to 'Edit', edit_procedure_path(procedure) %></td>
        <td><%= link_to 'Destroy', procedure, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Procedure', new_procedure_path %>
